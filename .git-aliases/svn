[alias]
    sbr = svn branch
    srm = !sh -c 'git checkout $1 && svn rm $(git svn info --url) -m "Delete branch $1" && git checkout - && git branch -r -d $1' -
    spush = svn dcommit
    spull = svn rebase
